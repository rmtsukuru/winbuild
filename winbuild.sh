#!/bin/sh
echo "Running Bash installation script... (hold tight, almost done with everything!)"

# TODO Add commands for setting everything up to pick up where winbuild.cmd left off

# This should already have winlibs installed at this point? We'll see
# Correction: winlibs should already be updated, now needs to be moved into correct directories and set up export
# so the PATH is set correctly (see section on dotfiles below for adding that in bashrc/bash_profile)

# This includes downloading and copying over dotfiles
# TODO add dotfiles (esp. bashrc, bash_aliases, and vimrc) to this repo for easy downloading
